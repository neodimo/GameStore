# GameStore task log

## 2026-08-24 — complete USA PlayStation catalog and launch-fresh discovery

- **What was done:** Replaced the 268-title selected preview with a reproducible import of 1,462 English-language USA retail PlayStation records from pinned OpenVGDB v29.0 metadata. Demos, magazine samplers, later discs, and duplicate revisions are excluded/collapsed; previously approved Europe-only English releases and explicitly documented Japan-only English translations remain when no USA title supersedes them. OpenVGDB catalog descriptions are used only when accompanied by their per-game reference URL (586 USA records); the remaining 876 show an explicit source-copy unavailable state. Every unsourced editorial quote was removed. Discover now chooses four of nine category recipes and six qualifying games per category once per app launch, so the landing shelves and their contents rotate without shifting during the session.
- **Artifacts:** Released at commit `005d2eef69c4055f0b8758711b371f3d2fb2d8d5`, tag `v0.12.0`: `https://github.com/neodimo/GameStore/releases/tag/v0.12.0`. Published assets: Windows installer (92,540,647 bytes), blockmap (96,420 bytes), Linux AppImage (123,071,402 bytes), Debian package (84,148,834 bytes), and both updater manifests. Source artifacts are `src/ps1UsCatalog.ts` (generated compact metadata), `scripts/import-openvgdb-ps1.py` (pinned reproducible importer), `src/catalog.ts`, `src/App.tsx`, `src/style.css`, tests, `README.md`, and `CONTEXT.md`. No ROMs, screenshots, videos, or provider credentials were added.
- **State:** Released. All 95 tests pass; TypeScript lint, web/Electron production builds, source/bundle/package media-light guards, and `git diff --check` pass. Core verification run `32702223317` passed in 40s; release run `32702225084` passed Linux in 2m45s and Windows in 3m21s. OpenVGDB does not provide reference-backed description copy for every title, and those gaps are deliberately visible rather than filled with generated text.
- **Next owner + concrete artifact:** Omid installs or updates to v0.12.0, checks the Discover count, relaunches twice to confirm shelf rotation, and opens games both with and without a **Catalog description source** link.
- **Failure mode:** The prior expansion called a hand-selected 268-game subset a broader library, populated many entries with synthetic one-line descriptions, and attached a generic synthetic curator quote. Full-catalog work must begin from a pinned retail metadata source, preserve per-record provenance, and leave missing editorial copy visibly missing.

## 2026-08-23 — expanded English-playable PlayStation library

- **What was done:** Opened the catalog beyond the original 100-title preview to a selected 268-game PS1 library. The 168 new records cover official English-language USA/Europe releases across RPGs, action, platformers, horror, fighting, rhythm, racing, shooters, puzzle, light-gun, and strategy games. Existing explicit Japanese English-translation records remain their own documented entries. The expansion intentionally avoids duplicate regional printings and does not masquerade as a full ROM-set listing.
- **Artifacts:** Released at commit `f7c494e5284bdfe681412f09a83bc385a7188fc4`, tag `v0.11.0`: `https://github.com/neodimo/GameStore/releases/tag/v0.11.0`. Published assets: `GameStore-Setup-0.11.0-x64.exe`, `.exe.blockmap`, `GameStore-0.11.0-x86_64.AppImage`, `GameStore-0.11.0-amd64.deb`, `latest.yml`, and `latest-linux.yml`.
- **State:** Released. Local validation passed: 88 tests, TypeScript lint, production web/Electron builds, source/bundle media-light guards, and `git diff --check`. Release CI run `32685842917` passed: Linux in 3m00s and Windows in 3m41s. Existing runtime artwork/media matching continues to resolve on demand; it has not been bulk-cached or bundled.
- **Next owner + concrete artifact:** Omid installs the v0.11.0 build and checks Discover's 268 PS1 games; Gonzo owns any reported inclusion, artwork, or matching correction against `src/ps1Expansion.ts`.

## 2026-08-23 — visible Real-Debrid preparation state

- **What was done:** Repaired the misleading collection-download state exposed by Vib-Ribbon. The UI previously showed `Downloading 0%` while the main process was adding the selected torrent, selecting the exact file, and waiting for Real-Debrid to cache it; there are no downloadable bytes during that provider-side work. Downloads now say **Preparing download** and receive live provider status (`added`, selected, then provider state plus elapsed wait) until a direct file link exists. The button only changes to byte-percent download once data is flowing. The existing six-minute bounded provider wait remains in force and returns a retryable error instead of waiting indefinitely.
- **Artifacts:** `electron/downloadManager.ts`, `src/App.tsx`, `src/vite-env.d.ts`, `CONTEXT.md`, `package.json`, and `package-lock.json`. Pending commit/release at this entry.
- **State:** Local implementation is done: 87 tests, TypeScript lint, and production web/Electron build passed. Live Real-Debrid collection preparation remains account-gated, but this exact prior state is covered by the new main-process progress contract.
- **Next owner + concrete artifact:** Gonzo releases v0.10.6. Omid retries Vib-Ribbon and should see the provider preparation message immediately; if Real-Debrid does not reach a direct download, send the exact final provider state text shown beneath the progress rail.
- **Failure mode:** The renderer initialized collection acquisition as `Fetching <filename>…` / `Downloading 0%`, but the first byte-progress event only arrives after Real-Debrid has completed torrent preparation. That made a normal provider queue/cache phase indistinguishable from a hung client.

## 2026-08-23 — cached connected-device library badges

- **What was done:** Added a green drive badge on catalog box art when an exact normalized title match exists in the configured MiSTer/SuperStation PSX library. The app performs one shallow SFTP listing of `<games root>/PSX` after a device is configured, persists only the folder-name inventory locally, and maps it to catalog IDs in the renderer. Card painting and scrolling make no network requests. A Settings-level **Refresh device library** action supports a deliberate rescan. The current scope is inventory/visibility only; existing cart checkout remains the add-to-device path and no remote deletion/overwrite behavior was introduced.
- **Artifacts:** `electron/fpgaInventory.ts`, `electron/fpgaInventory.test.ts`, `electron/main.ts`, `electron/preload.ts`, `src/App.tsx`, `src/style.css`, `src/vite-env.d.ts`, and `CONTEXT.md`. Local working tree pending commit/release at this entry.
- **State:** Local implementation is done and validated with 87 tests, TypeScript lint, and production web/Electron builds. Physical MiSTer/SuperStation inventory is unverified until a configured device is online.
- **Next owner + concrete artifact:** Gonzo releases the next patch. Omid configures the device in **Settings → SuperStation / MiSTer**, then checks that an existing `PSX/<title>` folder earns the green drive badge; use **Refresh device library** after copying games outside GameStore.

## 2026-08-23 — EmuMovies FTP credential clarification

- **What was done:** Corrected the EmuMovies account flow to call for the provider's separately generated FTP/file-server username and password, rather than falsely presenting the emumovies.com website login as interchangeable. Authentication rejections now explain that a generated FTP username commonly ends in `@emumovies-fileserver.com` and explicitly do not diagnose membership tier from a 530/430 response. Settings copy and placeholders now make the required credentials clear.
- **Artifacts:** Released in commit `c5b17bf1fe30cdc0c365404a5d6a4196ae158844`, tag `v0.10.2`: `https://github.com/neodimo/GameStore/releases/tag/v0.10.2`. Verified public assets: `GameStore-Setup-0.10.2-x64.exe` (92,431,598 bytes), `.exe.blockmap` (96,250 bytes), `GameStore-0.10.2-x86_64.AppImage` (122,936,377 bytes), `GameStore-0.10.2-amd64.deb` (84,052,396 bytes), `latest.yml`, and `latest-linux.yml`. No user credentials were read, logged, or stored by this work.
- **State:** Released. Local `npm test -- --run` passed 86 tests, TypeScript lint, production web/Electron build, and `git diff --check` passed; release CI run `32672682820` passed on Linux and Windows. Live FTP authentication awaits Omid entering the generated file-server credentials.
- **Next owner + concrete artifact:** Omid installs `GameStore-Setup-0.10.2-x64.exe` and uses **Settings → EmuMovies account** with the FTP/file-server credentials supplied by EmuMovies, rather than the website email/password.
- **Failure mode:** Version 0.10.0 labeled the fields as EmuMovies website credentials and converted any FTP authentication failure into a claim that the account lacked Supporting/Lifetime entitlement. EmuMovies website and FTP credentials can be distinct, so this was both misleading and wrong.

## 2026-08-23 — v0.10.1 library-export hierarchy polish

- **What was done:** Removed the prominent header-level **Export shelf** control. The same Favorites-only JSON export now lives in **Settings → Library export**, where its copy states exactly what it includes and excludes.
- **Artifacts:** `src/App.tsx`, `package.json`, and `package-lock.json`; release evidence will be recorded here after CI publishes v0.10.1. Local source is on `fix/move-library-export`.
- **State:** Implementation and local validation are done: 86 tests, TypeScript lint, production web/Electron build, media-light guard, and diff check passed. Release pending.
- **Next owner + concrete artifact:** Gonzo merges/tags/verifies v0.10.1; Omid can find the action under **Settings → Library export** in the released build.

## 2026-08-23 — v0.10.0 primary media, EmuMovies sign-in, collection index, and cart UX

- **What was done:** Rebuilt expanded game details around a larger left-hand cover with description and facts directly beneath it, while the right column now holds acquisition, preview, and a large gameplay gallery. Screenshots are pinned to the selected primary release instead of pooled across regions; captured preview frames expand gameplay coverage. Added a Settings-based EmuMovies sign-in, OS-backed encrypted credential storage, FTP entitlement probing/index caching, exact No-Intro/Redump-style video-snap matching, and local snap caching. Internet Archive remains a strict fallback that streams only the preview range through a local range proxy; it does not silently download a longplay. Added an application-media protocol so cached frames/images render reliably in Electron. Configured collection torrents now create a persistent manifest at save time and normal searches reuse it; Add to cart promotes primary-region candidates.
- **Artifacts:** Implementation merged in PR #17 at `ce0bef16f301e8ad27606c765e27ec84d9b6d9e0`; public release: `https://github.com/neodimo/GameStore/releases/tag/v0.10.0`; successful Windows/Linux workflow: `https://github.com/neodimo/GameStore/actions/runs/32669516833`. Verified assets: `GameStore-Setup-0.10.0-x64.exe` (92,431,566 bytes), `.exe.blockmap` (96,098 bytes), `GameStore-0.10.0-x86_64.AppImage` (122,936,314 bytes), `GameStore-0.10.0-amd64.deb` (84,040,670 bytes), `latest.yml`, and `latest-linux.yml`. Visual-QA scratch (not committed): `/tmp/gamestore-v010-click-detail.png`, `/tmp/gamestore-v010-media-rendered.png`, `/tmp/gamestore-v010-emumovies-settings.png`. The rendered Silent Hill gallery showed USA-only full-size gameplay frames alongside the new cover/facts layout.
- **State:** Released. Both CI jobs passed. Live EmuMovies authentication remains unverified because no account was entered. `npm test` passed 86 tests; TypeScript lint, production web/Electron build, media-light guard, diff check, and production dependency audit passed with zero audit vulnerabilities.
- **Next owner + concrete artifact:** Omid installs `GameStore-Setup-0.10.0-x64.exe`, enters EmuMovies credentials in **Settings → EmuMovies account**, and reports the displayed login/index error if the membership tier or FTP path differs.
- **Failure mode:** The former 120 MB cache ceiling made matched multi-gigabyte longplays appear perpetually unavailable. The fallback now range-streams a short loop, and the preferred EmuMovies path supplies an exact, compact snap. Cached local screenshots previously used raw `file://` URLs that the renderer could not consistently display; `gsmedia://asset` now serves only app media-cache files through the main process.

## 2026-08-23 — GameStore release-default workflow correction

- **What was done:** Recorded Omid's standing instruction that completed approved GameStore changes should proceed through merge, version tag, Windows/Linux release CI, and published-asset verification by default. Evidence: the v0.9.0 work was initially left at a green draft PR despite Omid's “proceed”; Omid explicitly corrected that interpretation.
- **Artifacts:** `CONTEXT.md` release-workflow section and this TASKLOG entry were merged through PR #15. Public release: `https://github.com/neodimo/GameStore/releases/tag/v0.9.0`; successful release run: `https://github.com/neodimo/GameStore/actions/runs/32665286167`.
- **State:** Done. PR #15 merged at `6acc7a5`; tag `v0.9.0` is published. Linux completed in 2m46s and Windows in 3m52s. Verified release assets: `GameStore-Setup-0.9.0-x64.exe` (92,409,660 bytes), `.exe.blockmap`, `GameStore-0.9.0-x86_64.AppImage` (122,887,280 bytes), `GameStore-0.9.0-amd64.deb` (84,017,508 bytes), `latest.yml`, and `latest-linux.yml`. The release is public, non-draft, and non-prerelease.
- **Next owner + concrete artifact:** Omid updates through the in-app updater or installs `GameStore-Setup-0.9.0-x64.exe`, then exercises one authenticated PSX ZIP through managed placement and physical MiSTer checkout.
- **Failure mode:** Treating the generic draft-PR safety default as the end of an explicitly authorized GameStore wrap-up. For this project, “proceed” after validated changes includes release unless Omid requests a review stop.

## 2026-08-23 — v0.9.0 managed download finalization and persistent MiSTer cart

- **What was done:** Implemented an ordered managed-library pipeline: provider bytes first land in a unique `.incoming` directory; ZIPs extract into a unique staging directory with path traversal and symlinks refused; the payload is inspected before placement; original filenames are preserved; disc/multi-file releases install under `Documents/GameStore/Games/<console>/<title>/`; single-file cartridge images install directly under `Documents/GameStore/Games/<console>/`; and the source ZIP is deleted only after complete successful placement. Every completed release is upserted into a disk-backed `library.json` MiSTer cart. The header now exposes the cart, managed item/file counts, removal without deleting local files, and one-click batch checkout. Checkout uses stored paths, removes each successfully transferred item from the queue, keeps library files, and leaves failed/unprocessed items queued. Evidence: real Electron QA at 1500×960 confirms the empty-cart header/popover hierarchy and fit. Four new library tests cover ZIP extraction/deletion with preserved `.cue`/`.bin` names, direct multi-file PSX placement, persistent queueing, direct single-file GBA placement, and partial-checkout retry semantics. All 62 tests, TypeScript lint, full web/Electron build, media-light source/bundle guards, diff check, and production dependency audit pass; the production audit reports zero vulnerabilities.
- **Artifacts:** Source was merged through PR #15 at `6acc7a5` and released publicly as `v0.9.0`. `/tmp/gamestore-cart-empty.png` and `/tmp/gamestore-cart-open2.png` are deliberate visual-QA scratch and are not committed.
- **State:** Released. Core managed-file lifecycle, persistent cart, PSX batch checkout, tests, builds, security audit, desktop visual QA, CI verification, and Windows/Linux packaging are done. Authenticated provider download → real ZIP → physical MiSTer checkout remains unverified because it requires Omid's encrypted account token and hardware. Non-PSX library placement is implemented and tested; remote route mapping for additional MiSTer console cores remains explicitly gated rather than guessed.
- **Next owner + concrete artifact:** Omid uses v0.9.0 to download one known PSX ZIP, verifies the resulting `Documents/GameStore/Games/PSX/<title>/` filenames and cart count, then checks out against the configured device. Report the exact cart/error state on failure; the retryable source of truth is `Documents/GameStore/library.json`.
- **Failure mode:** The previous flow downloaded archives directly into a title folder and offered per-game transfer, but had no extraction/finalization contract or durable queue. A first extraction dependency was rejected during this task because its production audit exposed an unresolved ZIP symlink traversal vulnerability; it was replaced with a streaming extractor that explicitly rejects traversal and symlink entries. Never accept an archive library with an unresolved traversal advisory in this path.

## 2026-08-23 — Visual/usability pass: browse intent and Favorites repair

- **What was done:** Ran the current v0.8.2 Electron shell at 1500×960 and exercised default discovery, navigation, empty Favorites, and typed search. Evidence: the initial capture showed that **Favorites** did not filter the catalog, active navigation did not follow the view, and search/filter results remained below unrelated curator shelves; the footer also still claimed `0.5 preview`. Implemented a real favorites-only view with an active nav state and useful empty-state guidance, hid curator shelves whenever the user is actively searching/filtering/browsing favorites, disabled the nonfunctional Platforms affordance with explanatory copy, corrected the footer version, added game-specific accessible favorite labels, and fixed singular/plural result counts. Retesting `silent hill` showed one result immediately below the filters with the correct `1 PlayStation game` heading; empty Favorites showed a direct route back to discovery. Automated evidence: 58 tests, TypeScript lint, full web/Electron production build, `git diff --check`, and media-light source guard all pass.
- **Artifacts:** `src/App.tsx` and `src/style.css` are committed and pushed with PR #15. `/tmp/gamestore-ux-before.png`, `/tmp/gamestore-ux-favorites.png`, and `/tmp/gamestore-ux-search.png` are deliberate visual-QA scratch and are not committed.
- **State:** Done locally and verified at the primary 1500×960 desktop size. Narrow-window and packaged Windows visual behavior remain unverified in this pass.
- **Next owner + concrete artifact:** Gonzo should use `src/App.tsx`, `src/style.css`, and the three `/tmp/gamestore-ux-*.png` captures for review/commit/release preparation; Omid can validate Favorites with his existing local shelf after the next packaged build.
- **Failure mode:** Navigation controls were styled like working destinations without owning distinct view state; Favorites merely reset one facet, while curated shelves ignored all browse intent. UI acceptance must exercise each primary navigation item and verify that the first content below the controls reflects that action.

## 2026-08-23 — v0.8.2 Real-Debrid raw torrent upload fix

- **What was done:** Corrected configured-collection uploads to Real-Debrid. The `.torrent` is now sent as the raw `PUT` request body with `Content-Type: application/x-bittorrent`; selective file-ID matching and selection remain unchanged. Added a regression test that asserts byte-for-byte body identity and rejects `FormData`. **Evidence:** Omid's packaged-app error was `torrent_file_invalid` / error 30 with “The file must be sent without a parameter, only raw data.” The new contract directly matches that requirement; 58 tests, the complete web/Electron build, and the media-light source guard pass.
- **Artifacts:** `electron/realDebrid.ts`, `electron/realDebrid.test.ts`, `electron/downloadManager.ts`, `package.json`, and `package-lock.json`. Intended status: committed, PR-merged, and released as v0.8.2 for the in-app updater; no credentials, torrent metadata, or game files are stored.
- **State:** Code path and request contract are fixed and locally verified. Authenticated Real-Debrid execution remains a user-side verification because Omid's token stays encrypted on his machine.
- **Next owner + concrete artifact:** Omid updates to v0.8.2, selects `Tekken 3 (USA).zip` again, and reports the next exact state/error if Real-Debrid rejects a later step. Gonzo uses the packaged-app result to verify selective ID mapping and resolved download behavior.
- **Failure mode:** v0.8.1 used browser-style multipart upload semantics for an endpoint that accepts only raw bencoded torrent bytes. The API therefore parsed the multipart envelope as torrent content and rejected it before file selection.

## 2026-08-23 — v0.8.1 configured collection indexing and exact-file debrid selection

- **What was done:** Closed the gap between debrid credentials and per-game downloading without embedding a commercial-ROM index in the public application. Settings now accepts a one-time HTTPS `.torrent` collection source. GameStore downloads metadata only (64 MB ceiling; localhost/private-network targets blocked), decodes its file manifest locally, fuzzy-matches the selected catalog title and preferred region, and shows exact filename/collection/size/confidence candidates in the game's Download rail. Choosing one uploads the configured torrent to Real-Debrid, waits for its file list, selects only the matching file ID(s), resolves the resulting links, and downloads allowed game/archive files into the organized local library. **Evidence:** 57 tests pass, including new bencode/file-ranking fixtures; TypeScript, production web/Electron builds, and media-light source/bundle checks pass. Official API documentation confirms Real-Debrid exposes file IDs plus selective `selectFiles`, and TorBox exposes cached file lists and per-file `requestdl` URLs. MiNERVA's public FAQ/search results confirm its system archives are distributed as multi-file torrents. Inference/unverified: authenticated end-to-end selection remains gated on Omid's locally stored Real-Debrid token and configured collection URL.
- **Artifacts:** `electron/collectionIndex.ts`, `electron/collectionIndex.test.ts`, `electron/downloadManager.ts`, `electron/main.ts`, `electron/preload.ts`, `src/App.tsx`, `src/style.css`, `src/vite-env.d.ts`, package metadata, `README.md`, and `CONTEXT.md`. Intended branch: `feat/collection-torrent-selection`; no torrent metadata or game files are committed or packaged.
- **State:** Real-Debrid configured-collection discovery and exact-file selection are implemented and locally validated short of authenticated API execution. TorBox remains available for supported direct host links; its collection-torrent path is not exposed until cached-availability and per-file ID responses are tested with Omid's account.
- **Next owner + concrete artifact:** Omid enters one PS1 collection `.torrent` URL under Settings, saves, opens a game, clicks **Find release in configured collections**, verifies the exact region/name candidate, and selects it. Report the exact displayed error if Real-Debrid rejects upload/selection. Gonzo uses that evidence to finish the parallel TorBox cached-file adapter.
- **Failure mode:** v0.8 described debrid downloading as usable but still required a per-game magnet/host link and selected every file from magnets. Debrid accounts resolve torrents; they do not discover them. The corrected architecture adds a separately configured collection index and performs exact file-ID selection. A source must be configured once; API keys alone cannot supply discovery.

## 2026-08-22 — v0.8 media-first library, private downloads, discovery, and 100 games

- **What was done:** Rebuilt expanded details around a large two-column screenshot gallery, substantial 4:3 short-loop video pane, narrow release-art column, and a right-side information/acquisition/source rail. Startup media audit now automatically caches strictly matched previews up to 120 MB while rejecting oversized longplays; local video loops the 8–45 second ambient segment. Added encrypted local Real-Debrid/TorBox credential settings and tests, a provider-backed queue with byte progress into `Documents/GameStore/Games/<title>`, Real-Debrid host-link and magnet resolution, TorBox host-link resolution, supported game-file allowlisting, RetroGameTalk fallback, and direct transfer of completed CHD/BIN/CUE library files. Added a rate-limited local `/24` SSH/SFTP scanner, likely/unknown candidate ranking, explicit selection, and `/media/fat` confirmation. Expanded the catalog from 30 to exactly 100 unique PS1 games and added Weird, Horror, Arcade, and RPG shelves. **Evidence:** 55 tests, TypeScript, production web/Electron builds, media-light source/bundle guards, and local Linux AppImage/DEB packaging passed. A real network scan completed in 4.97 seconds and returned four selectable SSH candidates. Chromium visual QA at 1600×1100 confirmed the media-first detail layout and credential settings. Evidence vs inference: Real-Debrid/TorBox live authenticated calls remain unverified because no account tokens were copied into this workspace; physical SuperStation transfer remains unverified until Omid selects and authenticates the device.
- **Artifacts:** Code/config is on branch `feat/v080-library-acquisition`; principal files are `electron/downloadManager.ts`, `electron/networkDiscovery.ts`, `electron/main.ts`, `electron/preload.ts`, `src/App.tsx`, `src/MediaGallery.tsx`, `src/mediaLibrary.ts`, `src/catalog.ts`, `src/style.css`, `src/vite-env.d.ts`, tests, `README.md`, `CONTEXT.md`, and package metadata. `/tmp/gamestore-v080.png`, `/tmp/gamestore-v080-detail.png`, and `/tmp/gamestore-v080-settings.png` are deliberate visual-QA scratch; local `release/` packages are ignored build artifacts pending CI publication.
- **State:** Implementation, local automated validation, visual QA, network-scan proof, and Linux packaging are done. Partial/unverified: TorBox magnet multi-file selection deliberately reports unsupported; TorBox supported-host link behavior and Real-Debrid authentication/download require Omid's local tokens; hardware identity/upload needs Omid's credentials. Oversized Archive.org longplays are intentionally refused as previews rather than downloaded.
- **Next owner + concrete artifact:** Gonzo publishes the branch through a PR and v0.8.0 Windows/Linux release. Omid updates, enters tokens under **Settings → Download providers**, tests both accounts, uses **Scan network**, selects the expected device and runs **Test connection**, then resolves one authorized host link or Real-Debrid magnet and uses **Send downloaded files to SuperStation / MiSTer**. Report the exact provider/device state text for any failure.
- **Failure mode:** The original media implementation treated a matched longplay as a preview and preloaded only its metadata, leaving users with multi-gigabyte manual downloads. v0.8 caps automatic previews at 120 MB, refuses oversized candidates, and keeps the UI honest while a dedicated short-form provider path is evaluated. The first browser detail QA also exposed a web-preview crash because `ensureGameMedia` assumed Electron IPC; it now renders stable empty media states outside the desktop shell.

## 2026-08-22 — 2×2 small-scale app icon

- **What was done:** Replaced the dense 3×3 GameStore application icon with a bold 2×2 grid of four large retro-package silhouettes while preserving the rounded dark library frame and synthwave palette. Generated a 1024×1024 master, applied transparent rounded corners, and quantized it below the standing UI-asset budget. Evidence: the icon remains legible at a real 32×32 launcher preview; all 54 tests, TypeScript validation, production web/Electron builds, and media-light source/bundle checks passed.
- **Artifacts:** `build/icon.png`, `package.json`, and `package-lock.json`. The final app icon is 358,711 bytes and committed; `/tmp/gamestore-icon-32.png` is deliberate visual-QA scratch. The source image was created with the built-in image-generation tool using the previous icon as the edit target.
- **State:** Done locally and prepared as patch release v0.7.1. Windows/Linux package rendering remains the release-CI gate.
- **Next owner + concrete artifact:** Omid updates to v0.7.1 and verifies the icon in the Windows taskbar, Start menu, and desktop shortcut. Use `build/icon.png` as the canonical master if platform-specific rendering needs adjustment.

## 2026-08-22 — Non-blocking startup media audit

- **What was done:** Added a startup media library that begins 600 ms after first paint, loads shared Libretro/Internet Archive indexes once, and processes the 30-game catalog through three throttled workers. It fills missing screenshots, resolves longplay match/size/cache state, deduplicates in-flight title requests, and supplies ready records to detail views. The header exposes queued/indexing/progress/ready/error states. Clearing media starts a fresh audit; full longplay binaries remain approval-gated. Evidence: 54 tests, TypeScript, production build, and media-light checks passed. Fresh-profile Electron QA remained interactive enough to open Incredible Crisis during the audit, cached 118 distinct screenshot files, resolved its 448 MB video offer, and reached **Media ready**.
- **Artifacts:** `src/mediaLibrary.ts`, `src/MediaGallery.tsx`, `src/App.tsx`, `src/style.css`, `README.md`, `CONTEXT.md`, `package.json`, and `package-lock.json`. Visual QA is deliberate scratch at `/tmp/gamestore-batch-active.png`; the throwaway runtime profile is `/tmp/gamestore-batch-profile2` and is not committed.
- **State:** Implementation and fresh-profile runtime QA done. Screenshot files are fetched in the background; video discovery/metadata is prefetched, while actual multi-hundred-megabyte/gigabyte video downloads still require explicit approval. Windows/Linux packaging remains the release CI gate.
- **Next owner + concrete artifact:** Omid updates to v0.7.0 and watches the header move through **Media N/30 → Media ready** while browsing. Settings → **Clear media** is the concrete rerun test.

## 2026-08-22 — Local screenshot rail and video cache

- **What was done:** Replaced the mutually exclusive empty media tabs with a simultaneous local-video pane and scrollable screenshot rail. Libretro `Named_Snaps`/`Named_Titles` are strictly matched, downloaded after a game is opened, and rendered from the disposable app cache. Internet Archive's 693-item PSX longplay index is cached and strictly matched; verified titles expose the smallest suitable MP4 derivative with its size, require **Download to cache**, report byte progress, and play only from the completed local file. Added cache bytes/path and clear controls to Settings. Evidence: 54 tests, TypeScript, production web/Electron build, media-light source/bundle guards, and production-dependency audit passed. Real Electron QA showed Incredible Crisis with five locally cached screenshots beside a correctly matched video offer, then downloaded Vib-Ribbon's complete 123,398,443-byte MP4 and played it from `file://` beside six cached screenshots.
- **Artifacts:** `electron/mediaCache.ts`, `electron/main.ts`, `electron/preload.ts`, `src/MediaGallery.tsx`, `src/mediaMatch.ts`, `src/mediaMatch.test.ts`, `src/mediaMatch.fixture.ts`, `src/App.tsx`, `src/style.css`, `src/vite-env.d.ts`, `README.md`, `CONTEXT.md`, `package.json`, and `package-lock.json`. Visual QA is deliberate scratch at `/tmp/gamestore-media-gallery.png` and `/tmp/gamestore-local-video.png`; fetched media is deliberate disposable runtime cache under the Electron user-data directory and is not committed.
- **State:** Implementation and end-to-end local playback validation done. Screenshot coverage was measured at 30/30 catalog games (131 matched images). Strict longplay matching finds 9/30; the remaining titles stay empty rather than accepting loose matches. Windows/Linux packaged builds remain CI gates.
- **Next owner + concrete artifact:** Omid updates to v0.6.0, opens Incredible Crisis, confirms the screenshot rail, then clicks **Download to cache** and verifies local playback. Use **Settings → Local media cache** to inspect/clear storage and report the exact state text if the video transfer fails.

## 2026-08-22 — Fuzzy box-art resolution and alternate-artwork deep search

- **What was done:** Replaced seeded exact-filename cover lookup with fuzzy resolution against the live Libretro thumbnail index. `electron/artIndex.ts` fetches and disk-caches the per-folder No-Intro filename list (7-day TTL, stale-on-failure); `src/artMatch.ts` scores it in the renderer using blended word/character similarity over normalized titles, with region reward and demotion of demos, prototypes and later discs. Added a right-click context menu (open details, search alternate box art, reset to automatic match, favorite) and an `ArtPicker` panel browsing box art / title screen / screenshot / TheGamesDB with per-candidate match scores; overrides persist locally with their source label. **Evidence (measured, live index of 9,339 files):** automatic resolution went from 13/30 seeded covers to 30/30, split 24 high / 6 medium / 0 low confidence, with zero incorrect picks — the six mediums are all genuine naming drift (`ParanoiaScape`→`Paranoia Scape`, `Baroque`→`Baroque - Yuganda Mousou`, `Tobal No. 2`→`Tobal 2`, `Cho Aniki`→`Chou Aniki`, `Internal Section`→`iS - Internal Section`, `Eggs of Steel`→ full subtitle). **Inference, not measured:** behaviour on catalogs beyond this 30-game slice. Tests 12/12, `tsc -b` exit 0, media-light guard passed at 0.78 MiB of 5 MiB. Visual QA drove the real Electron app under Xvfb at 1600×1000 with synthetic input: catalog grid, context menu, and deep-search panel all confirmed on screen.
- **Artifacts:** `src/artMatch.ts`, `src/artMatch.test.ts`, `src/artMatch.fixture.ts`, `src/artwork.tsx`, `src/ArtPicker.tsx`, `electron/artIndex.ts`, plus edits to `electron/main.ts`, `electron/preload.ts`, `src/App.tsx`, `src/catalog.ts`, `src/style.css`, `src/vite-env.d.ts`, `README.md`, `CONTEXT.md`. Intended status: committed on `feat/fuzzy-box-art`, merged by PR, released as v0.5.0. Visual-QA captures at `/tmp/gs-grid.png`, `/tmp/gs-menu2.png`, `/tmp/gs-picker3.png` are deliberate scratch, delivered to `#gamestore` and not committed. The coverage measurement script was scratch and was deleted after producing the numbers above.
- **State:** Done and visually verified locally. Unverified: the deep-search TheGamesDB tab still depends on a user-supplied API key and was not exercised; installer-level behaviour on Omid's Windows machine remains the user-side gate.
- **Failure mode:** The deep-search panel originally reused the auto-resolver's strict 0.6 match floor, so a *manual* browse returned only two candidates — the same decision rendered twice rather than a real alternate-art search. Caught only because visual QA opened the panel; unit tests were green throughout since the auto path was correct. Fixed by splitting the floors (`MATCH_FLOOR` 0.6 for automatic, `BROWSE_FLOOR` 0.3 for manual browsing) and locking the distinction with a test asserting the browse path returns strictly more while the auto path and top pick stay identical. **Rule: a strictness threshold tuned for an automatic decision is the wrong threshold for a human browsing surface — check every consumer when sharing a constant, and open the UI before calling a UI feature done.**
- **Next owner + concrete artifact:** Omid installs v0.5.0, right-clicks any game with wrong or missing art, and uses **Search alternate box art**. Report any title where the automatic pick is wrong along with what the panel offered instead, so the scoring weights can be tuned against a real miss.

## 2026-08-22 — RetroGameTalk fallback, box-art-grid icon, and FPGA transfer

- **What was done:** Added an unverified RetroGameTalk repository search fallback to every catalog record. Replaced the package icon with a purpose-built grid of abstract retro box-art cases. Added locally configured SFTP transfer for SuperStation One and MiSTer: connection testing, encrypted credential storage, CHD/BIN/CUE selection and validation, `/media/fat/games/PSX/<game>` grouping, directory creation, and live byte progress in the detail accordion. Evidence: catalog tests, TypeScript validation, production web/Electron build, media-light guard, and production-dependency security audit passed.
- **Artifacts:** `build/icon.png`, `electron/main.ts`, `electron/preload.ts`, `src/App.tsx`, `src/style.css`, `src/catalog.ts`, `src/vite-env.d.ts`, `package.json`, `package-lock.json`, `README.md`, and `CONTEXT.md`. The icon was generated with the built-in image tool from a no-text, no-character 3×3 abstract game-case prompt, then resized/optimized locally to 512×512 (384,690 bytes). Intended status: committed, pushed, and released as v0.4.0; generated installers remain ignored local/CI artifacts.
- **State:** Implementation and local validation done. A physical network write is unverified because no SuperStation One or MiSTer was reachable from this environment; the in-app connection test and Omid's first hardware transfer are the acceptance gate. RetroGameTalk links are deliberately labeled unverified because repository entries can disappear or rename.
- **Next owner + concrete artifact:** Omid installs v0.4.0, enters the device host and credentials under Settings, runs **Test connection**, then uses **Send to SuperStation / MiSTer** on a legally obtained CHD or complete BIN/CUE set. Report the exact displayed error and device firmware if the hardware test fails.

## 2026-08-22 — Background in-app updater

- **What was done:** Added a header updater flow with check, update-available, background download progress, ready, restart, current, unsupported, and error states. Windows NSIS and Linux AppImage builds use GitHub Releases through `electron-updater`; `.deb` installs explicitly defer to the package manager. The renderer receives status through a narrow preload IPC bridge, and release CI now uploads updater manifests and blockmaps with installers. Evidence: catalog tests, TypeScript validation, production web/Electron build, media-light guard, and runtime-only dependency audit passed.
- **Artifacts:** `electron/updater.ts`, `electron/main.ts`, `electron/preload.ts`, `src/App.tsx`, `src/style.css`, `src/vite-env.d.ts`, `package.json`, `package-lock.json`, `.github/workflows/release.yml`, `README.md`, and `CONTEXT.md`. Intended repository status: committed/pushed on `feat/in-app-updater`, merged through GitHub, and released as v0.3.1.
- **State:** Implementation complete locally. A true end-to-end update requires two published versions; v0.3.1 establishes the updater-capable baseline, and its check/no-update path can be tested immediately after release. The next release will exercise download/stage/restart from v0.3.1.
- **Failure mode:** The first packaged AppImage smoke test exposed that a default import from `electron-updater` compiled but resolved undefined under the CommonJS Electron bundle. The updater now uses the library's named `autoUpdater` import; packaged launch is a mandatory release gate because the renderer/dev build cannot catch this interop failure.
- **Next owner + concrete artifact:** Gonzo publishes v0.3.1 and verifies its updater metadata assets. Omid installs v0.3.1 once; the next GameStore release should then arrive through the header button without a visible installer.

## 2026-08-22 — Mockup-aligned UI and release-ratio artwork

- **What was done:** Rebuilt the live desktop shell around the approved mockups: fixed navigation rail, global search, platform and filter bars, dense editorial shelf, six-column catalog, and an anchored three-part detail accordion. Cover frames now adopt each loaded scan's intrinsic aspect ratio and render with containment, preventing the former universal 3:4 crop. Added a local TheGamesDB API-key setting and explicit front-cover candidate search/apply flow; Libretro remains the zero-configuration source. Evidence: catalog tests, TypeScript validation, production web/Electron build, media-light source guard, and a 1600×1000 Linux Chromium visual smoke test passed.
- **Artifacts:** `src/App.tsx`, `src/style.css`, `electron/main.ts`, `electron/preload.ts`, `src/vite-env.d.ts`, `README.md`, and `CONTEXT.md`. Visual QA capture is deliberate temporary scratch at `/tmp/gamestore-v003.png`; generated bundles remain ignored local artifacts. Intended repository status: commit/push through PR #4, followed by the v0.3.0 release pipeline.
- **State:** Implementation complete locally. TheGamesDB live lookup still needs user-key validation against Omid's account/quota; no API key was requested, exposed, or committed. Provider overrides are local and explicit; a fuller append-only override-history UI remains future work.
- **Next owner + concrete artifact:** Gonzo publishes and verifies PR #4 and the v0.3.0 Windows/Linux release artifacts. Omid adds the key under **Settings → Artwork scrapers**, opens a game, and tests **Find official box art** with an exact-region title.

## 2026-08-22 — Media-light repository and package guard implemented

- **What was done:** Enforced the standing lightweight-core rule in package configuration and release CI. The source guard rejects bundled video, catalog/media archives, artwork outside explicit app-owned UI paths, and oversized UI assets. Separate checks enforce tracked-core, compiled web/Electron bundle, Windows installer, Linux AppImage, and Debian package budgets. Evidence: tests, TypeScript validation, production build, bundle checks, and current Linux artifact checks passed; a deliberate `data/covers/should-fail.png` fixture was correctly rejected and then removed.
- **Artifacts:** `scripts/check-media-light.mjs`, `config/media-light.json`, `package.json`, `.github/workflows/release.yml`, `README.md`, and `CONTEXT.md`. Intended status: committed and pushed through the repository workflow; generated `dist/`, `dist-electron/`, and `release/` outputs remain ignored/local build artifacts.
- **State:** Done. Current measurements: tracked core 0.35 MiB of 5 MiB; web bundle 0.22 MiB of 3 MiB; Linux AppImage 115.76 MiB of 130 MiB; Debian package 158.58 MiB of 180 MiB. Windows size enforcement will run on the Windows CI runner.
- **Next owner + concrete artifact:** Gonzo maintains thresholds and allowlists in `config/media-light.json` when dependencies or app-owned UI assets change; pull-request CI in `.github/workflows/ci.yml` and package checks in `.github/workflows/release.yml` are the acceptance gates.

## 2026-08-22 — Media-light distribution rule adopted

- **What was done:** Recorded Omid's standing rule that GameStore's repository and downloadable installers stay lightweight. Per-game artwork, screenshots, thumbnails, and video are acquired after installation through built-in auto-scrapers/providers; only code, compact metadata, provider logic, and small app-owned UI assets ship. Cache behavior, user control, provenance preservation, video streaming, and future CI size guards are now explicit requirements.
- **Artifacts:** `README.md` and `CONTEXT.md`, committed/pushed project documentation.
- **State:** Done as a durable product invariant. The current vertical slice already loads remote media rather than bundling a media archive; scraper orchestration, cache controls, and CI size-budget enforcement remain future implementation work.
- **Next owner + concrete artifact:** Gonzo uses the distribution invariant in `CONTEXT.md` when implementing the media-provider/cache layer; the acceptance artifact is a CI check plus an in-app media/cache settings screen.

## 2026-08-22 — Linux parity and v0.2 packaging

- **What was done:** Added Linux as a first-class release target without removing or conditionalizing catalog features. Electron now uses native window chrome outside macOS, and the release workflow independently tests and packages Windows and Linux. Evidence: catalog tests, TypeScript validation, production bundling, AppImage packaging, Debian packaging, runtime-only dependency audit, and a real Linux Electron visual smoke test passed locally.
- **Artifacts:** Source is committed and pushed in GitHub release `v0.2.0` at `https://github.com/neodimo/GameStore/releases/tag/v0.2.0`. Release assets are `GameStore-0.2.0-x86_64.AppImage`, `GameStore-0.2.0-amd64.deb`, and `GameStore-Setup-0.2.0-x64.exe`. Visual-QA captures are deliberate temporary files in `/tmp/gamestore-linux-qa.LZwZTL/` for Discord delivery.
- **State:** Done. GitHub's Linux and Windows jobs independently passed install, catalog tests, TypeScript validation, native packaging, and release upload. The packaged Linux app was also launched and visually inspected locally. Installers remain unsigned; installation on Omid's exact Linux machine is the remaining user-side confirmation.
- **Next owner + concrete artifact:** Omid runs either `GameStore-0.2.0-x86_64.AppImage` or `GameStore-0.2.0-amd64.deb` from the v0.2.0 release and reports distro/desktop-specific issues in `#gamestore` with a screenshot.

## 2026-08-22 — GameStore v0.1.1 released

- **What was done:** Published the public `neodimo/GameStore` repository and production GitHub release `v0.1.1`. GitHub's Windows runner passed install, catalog tests, TypeScript validation, production bundling, NSIS packaging, and release upload.
- **Artifacts:** Source is committed/pushed at `https://github.com/neodimo/GameStore` (HEAD `309c1f5` before this log-only follow-up). Installer `GameStore-Setup-0.1.1-x64.exe` is attached to `https://github.com/neodimo/GameStore/releases/tag/v0.1.1`; SHA-256 `1a247a83c263279326dd226d77b9de3a28e767e9c0ec782c3d87a464beda0ce3`.
- **State:** Done. Windows installer was produced and uploaded by a successful Windows CI run. It is currently unsigned, so Windows SmartScreen may show an unknown-publisher warning. Installation on Omid's specific machine remains user verification.
- **Next owner + concrete artifact:** Omid downloads and runs `GameStore-Setup-0.1.1-x64.exe`; report UI/data issues in `#gamestore` with the affected title/filter and a screenshot.

## 2026-08-22 — Windows release packaging correction

- **What was done:** Corrected electron-builder so tagged CI builds produce the NSIS installer without electron-builder attempting its own unauthenticated parallel publication. Artifact publication remains owned by the explicit GitHub Release action.
- **Artifacts:** `package.json`, `package-lock.json`, and `.github/workflows/release.yml` in the GameStore repository. Intended committed/pushed release fix.
- **State:** Fix prepared; the replacement Windows runner and attached installer require verification.
- **Next owner + concrete artifact:** Gonzo tags `v0.1.1`, watches the GitHub Actions run, and verifies `GameStore-Setup-0.1.1-x64.exe` on the release page.
- **Failure mode:** electron-builder detects Git tags and defaults to auto-publish when a `publish` config exists; without `GH_TOKEN` in its process it fails after successfully building. Always pass `--publish never` when a later workflow step owns release upload.

## 2026-08-22 — v0.1 Windows vertical slice implemented

- **What was done:** Built the first GameStore desktop vertical slice in Electron, React, and TypeScript. Evidence: 30 unique PS1 catalog fixtures; search and deep filters; curated shelves/facets; inline detail accordion; translation/base-release records; stateful source links; click-to-play video and explicit missing-media states; responsive layouts; favorites and JSON export. Automated catalog tests, TypeScript validation, production build, runtime dependency audit, and desktop/narrow visual smoke tests pass. The runtime dependency audit reports zero known vulnerabilities.
- **Artifacts:** Application source and build workflow live throughout `projects/GameStore/`; production web bundle in `projects/GameStore/dist/` is deliberate local build output and ignored by Git. Visual QA captures are deliberate temporary files at `/tmp/gamestore-desktop.png` and `/tmp/gamestore-narrow.png`. GitHub publication/release is the remaining step for this entry.
- **State:** Implementation and local validation done; Windows installer remains unverified until the GitHub Windows runner finishes packaging it. Several upstream cover URLs deliberately fall back to labeled missing-art cards when exact filenames do not match; no questionable regional art is substituted.
- **Next owner + concrete artifact:** Gonzo publishes/tag/releases the repository using `.github/workflows/release.yml`, then verifies the GitHub Actions run and attached `.exe` artifacts. Omid installs the NSIS `.exe` from the GitHub release.

## 2026-08-22 — Project identity established

- **What was done:** Omid selected **GameStore** as the project and planned repository name. The workspace project record was created, and the existing Discord discussion channel was designated for rename/move to `#gamestore` under `Projects`.
- **Artifacts:** `projects/GameStore/README.md`, `projects/GameStore/CONTEXT.md`, and this task log. Local workspace files; no Git repository or remote has been created.
- **State:** Done for naming and workspace identity. Planning remains in progress; implementation is explicitly unstarted.
- **Next owner + concrete artifact:** Bert/Gonzo should produce plan v002 and revised edge-state mockups using `projects/GameStore/README.md` and `projects/GameStore/CONTEXT.md`; Omid approves before the 30-case spike.
## 2026-08-23 — v0.10.3 UX/performance pass

- **What was done:** Audited the production desktop UI and exercised catalog-to-detail interaction under Xvfb at 1600×1000. Removed the header media-audit status and the launch-time 100-title media crawl: browsing no longer triggers a catalog-wide run of provider probes and screenshot-cache writes. Media resolves only for a title the user opens, with its screenshot cache and video-provider resolution started in parallel. Corrected the detail anchor so its title lands below the sticky header rather than hidden behind it. The footer now reports the release version accurately.
- **Artifacts:** `src/App.tsx`, `src/mediaLibrary.ts`, `src/style.css`, `package.json`, `package-lock.json`, and `CONTEXT.md`. Visual-QA captures at `/tmp/gamestore-ux-v0103-wide.png`, `/tmp/gamestore-ux-v0103-detail.png`, and `/tmp/gamestore-ux-v0103-detail-fixed.png` are deliberate local scratch and uncommitted.
- **State:** Done locally; production web/Electron build, TypeScript lint, 86 tests, source/bundle media-light checks, `git diff --check`, and production dependency audit (0 vulnerabilities) pass. Exact production installer/CI verification follows the pushed v0.10.3 release.
- **Next owner + concrete artifact:** Gonzo owns publishing and verifying v0.10.3. Omid's acceptance artifact is the released installer and a real Windows browse/open interaction, especially on a cold media cache.
## 2026-08-23 — v0.10.4 dark scroll surfaces

- **What was done:** Added application-wide dark scrollbars with slim rounded thumb/track treatment for Chromium and Firefox, smooth scrolling, stable scroll gutters on dynamic panes, and a reduced-motion fallback. This covers the document, settings, cart, galleries, filters, and artwork picker rather than leaving any of them with light browser chrome.
- **Artifacts:** `src/style.css`, `src/App.tsx`, `package.json`, and `package-lock.json`; visual capture is deliberate scratch at `/tmp/gamestore-v0104-scrollbars.png` and will be posted to Discord. Intended source state: committed, pushed, and released as v0.10.4.
- **State:** Local validation passed: 86 tests, TypeScript lint, production build, source/bundle media-light checks, and `git diff --check`. Visual QA confirms the rounded dark scrollbar inside the scrollable Settings modal at 1600×1000. Release CI remains the publish gate.
- **Next owner + concrete artifact:** Gonzo publishes and verifies the v0.10.4 GitHub Release. Omid can inspect the attached settings capture and released desktop build.
## 2026-08-23 — v0.11.1 detail containment, release-verified screenshots, and EmuMovies connection correction

- **What was done:** Fixed expanded-detail scroll placement by measuring the live sticky header before scrolling, keeping the detail heading and large cover below global search. Reordered header actions to **Check for updates** then **MiSTer cart**. Tightened screenshots to the selected release: untagged images are excluded and gameplay captures are shown only when sourced from an exact, region-matched EmuMovies snap; Internet Archive can still provide a looping preview but cannot populate a falsely labeled regional gallery. Corrected EmuMovies configuration from the prior generated-credential/`ftp.emumovies.com` assumption to the provider’s published `files.emumovies.com:21` using the member’s forum username/password; TLS is attempted first and Plain FTP is the connection fallback.
- **Artifacts:** `src/App.tsx`, `src/MediaGallery.tsx`, `src/mediaLibrary.ts`, `src/mediaMatch.ts`, `src/mediaMatch.test.ts`, `electron/emuMovies.ts`, `package.json`, `package-lock.json`, and `CONTEXT.md`. `/tmp/gamestore-v0111-header.png` and `/tmp/gamestore-v0111-detail*.png` are deliberate local visual-QA scratch and uncommitted.
- **State:** Local implementation complete and validated: 89 tests, TypeScript lint, production web/Electron build, source/bundle media-light guards, and `git diff --check` pass. Electron visual inspection at 1600×1000 confirms the corrected header order; published release/CI verification remains pending tag execution.
- **Next owner + concrete artifact:** Gonzo completes the v0.11.1 release from the commit created for these files, then Omid updates and signs into **Settings → EmuMovies account** with the forum credentials shown by EmuMovies. Use the Syphon Filter 2 details pane to verify that no Spanish Archive frames appear in the USA gallery.
- **Failure mode:** The previous gallery treated any matching Archive longplay as primary-release gameplay, but Archive titles do not reliably identify region. Syphon Filter 2 therefore displayed Spanish frames under a USA header. The old EmuMovies guidance also named the wrong host and credential type.
## 2026-08-23 — v0.11.2 sidebar containment, single-printing galleries, and EmuMovies FTP tree discovery

- **What was done:** Traced the persistent overlap to the global `aside` selector: it applied the navigation rail’s full-height sticky positioning and z-index to the nested game-detail rail. Scoped those rules to `.shell > aside`, keeping cover/details content in normal flow below search. Changed galleries to one exact regional retail printing, excluded demos/betas/prototypes/samples, retained gameplay across that printing’s discs, and limited title screens to one with Disc 1 preferred. Replaced EmuMovies’ system-first-only directory lookup with bounded, directed discovery supporting both `/Sony PlayStation/Video_Snaps_HD` and `/Official/Video Snaps (HQ)/Sony PlayStation`, with later Sony systems excluded. Successful authentication without a discovered folder now stops with the probe’s useful status instead of invoking indexing and surfacing a raw IPC exception.
- **Artifacts:** `src/style.css`, `src/mediaMatch.ts`, `src/mediaMatch.test.ts`, `src/App.tsx`, `electron/emuMovies.ts`, `electron/emuMovies.test.ts`, `package.json`, `package-lock.json`, and `CONTEXT.md`. `/tmp/gamestore-v0112-*.png` is deliberate local visual-QA scratch and uncommitted.
- **State:** Implementation complete locally. Evidence: 92 tests, TypeScript lint, production web/Electron build, source/bundle media-light guards, and diff checks pass. Real authenticated EmuMovies indexing remains user-side because credentials stay on Omid’s machine.
- **Next owner + concrete artifact:** Gonzo releases v0.11.2; Omid updates, signs in under **Settings → EmuMovies account**, and reports the displayed discovered-folder/index result. Syphon Filter 2 is the visual acceptance title for header containment and its reduced retail-only screenshot set.
- **Failure mode:** A broad element selector made every `<aside>` sticky at z-index 30, defeating the header’s z-index 20. Media matching treated every same-region disc/title/demo variant as one gallery. EmuMovies scanning assumed the system folder lived at FTP root, so a valid lifetime account with a quality-first Official tree authenticated and then falsely reported no PlayStation content.
